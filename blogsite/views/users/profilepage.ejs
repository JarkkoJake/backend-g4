<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <link rel="stylesheet" href="css/bootstrap.css">
    <link rel="stylesheet" href="css/styles.css">
</head>
<body>
    <div>
        <header>
            <h1 id="title">Profile of <%= loggedIn ? currentUser.username : " "%></h1>
            </nav>
            <div class="blogContainer">
                <div>
                    <img src="<%= currentUser.profilePicture != ' ' ? currentUser.profilePicture : 'images/päärynä.jpg' %>">
                    <p>Email: <%= currentUser.email ? currentUser.email : "no email" %></p>
                </div>
                <article>
                    <%= currentUser.bio ? currentUser.bio : "no bio" %>
                </article>
            </div>
        </header>
    </div>
</body>
</html>